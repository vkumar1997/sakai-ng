import { ItemAdditionalInfo } from './itemadditionalinfo.model';

describe('Itemadditionalinfo', () => {
  it('should create an instance', () => {
    expect(new ItemAdditionalInfo()).toBeTruthy();
  });
});
