import { ItemPropertyType } from './itempropertytype.model';

describe('Itempropertytype', () => {
  it('should create an instance', () => {
    expect(new ItemPropertyType()).toBeTruthy();
  });
});
